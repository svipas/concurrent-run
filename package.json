{
  "name": "concurrent-run",
  "description": "Simple, zero dependency, multiple commands runner in concurrent mode.",
  "version": "2.0.0",
  "main": "src/concurrentRun.js",
  "bin": {
    "concurrent-run": "src/cli.js"
  },
  "author": "svipas",
  "repository": "https://github.com/svipas/concurrent-run",
  "license": "MIT",
  "scripts": {
    "lint": "eslint .",
    "test": "jest"
  },
  "devDependencies": {
    "@types/jest": "^24.0.14",
    "eslint": "^5.16.0",
    "jest": "^24.8.0"
  },
  "keywords": [
    "multiple",
    "commands",
    "runner",
    "run",
    "concurrent"
  ]
}
