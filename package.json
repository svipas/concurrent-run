{
  "name": "concurrent-run",
  "description": "simple, zero dependency, commands runner in concurrent mode",
  "version": "1.0.0",
  "bin": "index.js",
  "license": "MIT",
  "repository": "https://github.com/svipben/concurrent-run",
  "scripts": {
    "lint": "eslint ."
  },
  "devDependencies": {
    "eslint": "^5.13.0"
  },
  "keywords": [
    "concurrent",
    "run",
    "commands"
  ]
}
