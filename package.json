{
  "name": "concurrent-run",
  "description": "Simple, zero dependency, multiple commands runner in concurrent mode.",
  "version": "2.2.3",
  "main": "src/concurrent-run.js",
  "bin": {
    "concurrent-run": "src/cli.js"
  },
  "author": "svipas",
  "repository": {
    "type": "git",
    "url": "https://github.com/svipas/concurrent-run.git"
  },
  "bugs": {
    "url": "https://github.com/svipas/concurrent-run/issues"
  },
  "license": "MIT",
  "scripts": {
    "lint": "eslint .",
    "test": "jest",
    "prepublishOnly": "node src/cli.js \"yarn lint\" \"yarn test\""
  },
  "devDependencies": {
    "@types/jest": "^24.0.19",
    "eslint": "^6.5.1",
    "jest": "^24.9.0"
  },
  "keywords": [
    "multiple",
    "commands",
    "runner",
    "run",
    "concurrent"
  ]
}
